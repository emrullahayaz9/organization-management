<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Online Event Manager - Superadmin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h2>Welcome, Superadmin</h2>
    </div>

    <div class="container mt-4">
        <div class="row">
            <div class="col-6">
                <h3>Add Venue</h3>
                <form action="/add-venue" method="post">
                    <div class="mb-3">
                        <label for="venueName" class="form-label">Venue Name</label>
                        <input type="text" class="form-control" id="venueName" name="name" required>
                    </div>
                    <div class="mb-3">
                       
                        <label for="venueLocation" class="form-label">Venue Location</label>
                        <select class="form-select" id="venueLocation" name="venueLocation" required>
                            <option>Please select option</option>
                            <% for (let i = 0; i < locations.length; i++) { %>
                                <option value="<%= locations[i].location_id %>"><%= locations[i].city %>, 
                                    <%= locations[i].state %>, 
                                    <%= locations[i].street %>, 
                                Door Number: <%= locations[i].door_number %>
                                <%= locations[i].location_id %>
                            </option>
                            <% } %>
                        </select>
                    </div>
                    <button type="submit" class="btn btn-primary">Add Venue</button>
                    <h4 class="mt-5">All Venues</h4>
                    <ul>
                    <% for (let i = 0; i < venues.length; i++) { %>
                        <li><%= venues[i].name %> <% if (venues[i].availability) { %>
                            Müsait Değil
                        <% %><% } else { %>
                         Müsait </li>
                    <% } } %>
                </ul>
                </form>

            </div>

            
            <div class="col-6">
                <h3>Add Location</h3>
                <form action="/add-location" method="post">
                    <div class="mb-3">
                        <label for="locationCity" class="form-label">City</label>
                        <input type="text" class="form-control" id="locationCity" name="city" required>
                    </div>
                    <div class="mb-3">
                        <label for="locationState" class="form-label">State</label>
                        <input type="text" class="form-control" id="locationState" name="state" required>
                    </div>
                    <div class="mb-3">
                        <label for="locationStreet" class="form-label">Street</label>
                        <input type="text" class="form-control" id="locationStreet" name="street" required>
                    </div>
                    <div class="mb-3">
                        <label for="locationDoorNumber" class="form-label">Door Number</label>
                        <input type="text" class="form-control" id="locationDoorNumber" name="door_number" required>
                    </div>
                    <button type="submit" class="btn btn-primary">Add Location</button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
